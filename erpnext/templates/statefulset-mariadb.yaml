{{- if (index .Values "mariadb-sts").enabled }}
{{- $m := (index .Values "mariadb-sts") }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "erpnext.fullname" . }}-mariadb-sts
  labels:
    {{- include "erpnext.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "erpnext.name" . }}-mariadb-sts
      app.kubernetes.io/instance: {{ .Release.Name }}
  serviceName: {{ include "erpnext.fullname" . }}-mariadb-sts
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "erpnext.name" . }}-mariadb-sts
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: mariadb
          image: "{{ $m.image.repository | default "mariadb" }}:{{ $m.image.tag | default "10.6" }}"
          imagePullPolicy: {{ $m.image.pullPolicy | default "IfNotPresent" }}
          env:
            - name: MARIADB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "erpnext.fullname" . }}
                  key: mariadb-root-password
          ports:
            - name: mysql
              containerPort: 3306
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: 3306
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 3306
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml ($m.resources | default dict) | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
            {{- if $m.myCnf }}
            - name: config
              mountPath: /etc/mysql/conf.d/my.cnf
              subPath: my.cnf
            {{- end }}
      volumes:
        {{- if $m.myCnf }}
        - name: config
          configMap:
            name: {{ include "erpnext.fullname" . }}-mariadb-sts
        {{- end }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: {{ $m.persistence.storageClass | default "longhorn" }}
        resources:
          requests:
            storage: {{ $m.persistence.size | default "8Gi" }}
{{- end }}
